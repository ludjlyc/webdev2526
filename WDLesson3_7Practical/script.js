/* Challenge 2: Create a function to serve as the event handler for the distance UI. Guidelines,
      1) Create variables and retrieve the information from the text inputs you created in Challenge 1.

function bulidingHeight(){
      let d= pareseFloat(document.getElementById("distance").value);
      let angle=                            ("angle")
      let op= document.GetElementById("output");
      let radiant= angle * Math.PI / 180;
      let height= d* Math.Tam(radiant);
      op.innerHTMl = 'Bled height is ${ht};

function distance (){

let x1= pareseFloat(document.getElementById("distance").value);
let angle= 
let y1= pareseFloat(document.getElementById




} 


      2) Perform the necessary calculations
      3) Display the results in the appropriate element
*/

/* Challenge 4: Create a function to serve as the event handler for the compound interest UI. Guidelines,
      1) Create variables and retrieve the information from the text inputs you created in Challenge 1.
      2) Perform the necessary calculations
      3) Display the results in the appropriate element
*/



